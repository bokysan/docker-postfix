# Outbound rate limiting is available but disabled by default. Enabling it
# requires an external Redis instance (rspamd stores counters there); point
# rspamd at it via /etc/rspamd/local.d/redis.conf (or the RSPAMD_redis__* and
# RSPAMD_ratelimit__* variables) and flip `enabled` to true.
enabled = false;
