# This image is an *outbound* relay: its job is to deliver mail, not to block
# it. Never reject or greylist a message based on its spam score -- rspamd is
# used here primarily for DKIM signing. If you want rspamd to actually reject
# outbound spam, override this file (mount your own) or set the appropriate
# RSPAMD_actions__* variables.
reject = null;
greylist = null;
