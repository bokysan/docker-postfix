# Disabled: this image is an outbound DKIM-signing relay and does not run
# inbound spam scanning / redis-backed modules by default. Mount your own
# local.d/rbl.conf (and configure redis where needed) to re-enable.
enabled = false;
